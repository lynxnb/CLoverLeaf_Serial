{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build tests (debug)",
      "command": "make",
      "args": ["test", "DEBUG=1"],
      "type": "shell"
    },
    {
      "label": "Build clover_leaf (debug)",
      "command": "make",
      "args": ["clover_leaf", "DEBUG=1"],
      "type": "shell"
    }
  ]
}
